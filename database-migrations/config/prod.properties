# Prod environment overrides
# All values must come from environment variables — no defaults
# Injected via Kubernetes secrets or ArgoCD config

DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_NAME=${DB_NAME}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}