# Dev environment overrides
# These values are injected via docker-compose environment block
# Never commit real credentials — use env vars

DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT:5432}
DB_NAME=${DB_NAME:justanotherapp}
DB_USERNAME=${DB_USERNAME:postgres}
DB_PASSWORD=${DB_PASSWORD:2810}